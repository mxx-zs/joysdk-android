<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/page_background">

    <Button
        android:id="@+id/openGameButton"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:background="@drawable/bg_open_game_primary"
        android:backgroundTint="@color/joy_primary"
        android:elevation="8dp"
        android:gravity="center"
        android:maxLines="2"
        android:minWidth="0dp"
        android:minHeight="0dp"
        android:padding="16dp"
        android:text="@string/open_game"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/screenModeGroup"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <RadioGroup
        android:id="@+id/screenModeGroup"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:baselineAligned="false"
        android:checkedButton="@id/fullScreenOption"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="8dp"
        android:paddingEnd="16dp"
        android:paddingBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <RadioButton
            android:id="@+id/fullScreenOption"
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_marginEnd="6dp"
            android:layout_weight="1"
            android:background="@drawable/bg_screen_option"
            android:button="@null"
            android:gravity="center"
            android:maxLines="1"
            android:text="@string/full_screen"
            android:textColor="@color/screen_option_text"
            android:textSize="15sp"
            android:textStyle="bold" />

        <RadioButton
            android:id="@+id/halfScreenOption"
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_marginStart="3dp"
            android:layout_marginEnd="3dp"
            android:layout_weight="1"
            android:background="@drawable/bg_screen_option"
            android:button="@null"
            android:gravity="center"
            android:maxLines="1"
            android:text="@string/half_screen"
            android:textColor="@color/screen_option_text"
            android:textSize="15sp"
            android:textStyle="bold" />

        <RadioButton
            android:id="@+id/largeHalfScreenOption"
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_marginStart="6dp"
            android:layout_weight="1"
            android:background="@drawable/bg_screen_option"
            android:button="@null"
            android:gravity="center"
            android:maxLines="2"
            android:text="@string/large_half_screen"
            android:textColor="@color/screen_option_text"
            android:textSize="15sp"
            android:textStyle="bold" />
    </RadioGroup>

</androidx.constraintlayout.widget.ConstraintLayout>
